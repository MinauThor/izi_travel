import 'package:flutter/material.dart';

class DropDeparture extends StatelessWidget {
  const DropDeparture({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(

    );
  }
}