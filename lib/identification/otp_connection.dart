import 'package:flutter/material.dart';

class OtpConnection extends StatefulWidget {
  const OtpConnection({Key? key}) : super(key: key);

  @override
  State<OtpConnection> createState() => _OtpConnectionState();
}

class _OtpConnectionState extends State<OtpConnection> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.white,
      body: SafeArea(
        child: SingleChildScrollView(
          
        ),
      ),
    );
  }
}